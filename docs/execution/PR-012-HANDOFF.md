# PR-012 handoff

State: integrated via [PR #18](https://github.com/xsparc/omniweft/pull/18), squash `d8f49dc97d97ef62600b58b299c24527b94bbb4c`, tree `1a339e85123dddd95601b90a9742c028ea9cecf5` matching reviewed delivery `58702f9852d121389cdc63375284c60a542c299e`. Historical checkpoints below are superseded by integration verification.

PR-011 is integrated with matching reviewed tree and all six required post-merge jobs passed, linked in its reconciled handoff. Existing worktrees are preserved. PR-012 implements bounded native supported-edit history through inverse Coordinator transactions: eight entries, four operations per entry, 16 KiB envelopes, generation-qualified tags and isolated-root transforms. Caller and private history-head revisions must match the live world. Rejections preserve history and world; explicit clear reconciles outside edits without restoring a snapshot. No remote history, new authority, persistence, GPU or physics capability is claimed.

All 33 Windows CTests passed. Independent review then identified that the direct example's --verify needed its own receipt/final-state checks; those checks were added and all three affected world tests passed. Native history has 98 assertions, the independent retained development oracle has 1666 assertions, and its selftest rejects 14 altered reports. Synthetic staging-hook rejection is tested; this is not a new policy lease integration proof. Existing policy and SDK regressions passed in the full suite. Local Linux is not_run; repaired-head hosted Windows/Linux checks passed as detailed below. Planning validation and diff whitespace checks passed.

The initial additional content-equivalence oracle paired the wrong two checkpoints and failed; correcting the test-only pair required no runtime change. The planning validator initially rejected a rewritten example missing literal adopted contract text; restoring that text resolved it without changing acceptance. These development failures are retained here rather than erased. OpenSteward's bundled static and dated strict checks reported a missing foreign-project registry, as explained in the plan; they are not claimed passed.

Independent architecture, source/oracle and CI reviews closed with no remaining runtime blockers; the example verifier and staging-hook terminology findings are resolved. Next: verify final documentation-head checks and its actual compiler versions; retain the explicit untested-servicing-version limitation if applicable. Await maintainer review/certification and manual integration once delivery checks pass. No merge authority or certification is inferred. Keep PR-013 proposed until integration.

## Hosted servicing repair

Draft [PR #18](https://github.com/xsparc/omniweft/pull/18) opened at runtime `a6b268a1b1f92ac4cd439203bd861c45729f4ad1`, tree `f668a21bae8ccf049050bdabc59e9ce74edb33b4`. The clean Windows oracle retained 1666 assertions and native 98. Independent archive audit passed 4580 static checks without rerunning native tests; the four JSON members preserve full fixture, canonical bytes, source/SDK bindings and actual build provenance.

Initial hosted Windows [native run](https://github.com/xsparc/omniweft/actions/runs/36024036881) and [renderer run](https://github.com/xsparc/omniweft/actions/runs/36024036783) failed before compilation because the runner's compiler serviced to MSVC 19.44.35229, while recorded pins allowed .35227/.35228. The exact [runner image](https://github.com/actions/runner-images/blob/win22/20260920.314/images/windows/Windows2022-Readme.md) identifies Visual Studio 17.14.37710.0. The bounded repair records .35229 as primary and retains the two previously verified exact alternates; compiler equality enforcement and all functional gates stay intact. Functional compatibility of .35229 remains unverified until complete required checks actually execute with that compiler; passing delivery checks on an accepted alternate do not establish it. No host tooling was changed. Runtime source is unchanged; local archive stays bound to a6b268a.

Independent exact servicing-pin review passed. The original runtime Linux native and renderer jobs and both planning jobs passed; Windows precompile failures remain recorded above. All six jobs must pass on the repaired delivery head.

## Verified repaired delivery

Delivery `f57b9185d678d926dc61603836c952308710ad6d`, tree `21ac4db42fe5120a1bf89444fc45c51908f7729f`, passed all six required checks: [native Windows/Linux](https://github.com/xsparc/omniweft/actions/runs/36024756369), [renderer Windows/Linux](https://github.com/xsparc/omniweft/actions/runs/36024756368), [planning Windows/Linux](https://github.com/xsparc/omniweft/actions/runs/36024756383). Both native manifests bind hosted checkout `c98305cd7e2b3af3f993a70270569cfbb0145975` to the identical delivery tree. Each platform retained 1666 oracle assertions and native 98, with source/SDK/artifact hashes and complete literal states independently checked. Linux used Clang 18.1.3. The successful Windows native and renderer jobs both ran all 33 CTests with MSVC 19.44.35228 on the older runner image 20260913.307.1.

The independently reviewed .35229 exact pin has only preflight provenance at this checkpoint; no successful .35229 functional run is claimed. Hosted rollout selected an older image for the successful jobs. The final head must still pass all required checks; inspect its actual retained compiler rather than equating a primary pin with execution. Do not force success, change runners/gates or loop reruns to conceal this distinction. Latest final-head state is linked in [PR checks](https://github.com/xsparc/omniweft/pull/18/checks); exact final links and compiler observations are retained in the PR body/private checkpoint. The public archive remains bound to unchanged runtime a6b268a.

## Integration verification

All six post-merge checks passed: [native](https://github.com/xsparc/omniweft/actions/runs/36251207114), [renderer](https://github.com/xsparc/omniweft/actions/runs/36251207118), [planning](https://github.com/xsparc/omniweft/actions/runs/36251207125). Both native manifests bind the actual merge/tree and retain 1666 oracle plus 98 native assertions with verified source/SDK/artifact bindings. Windows native and renderer actually exercised MSVC19.44.35229, with all 33 CTests passing; the earlier servicing limitation is now resolved. Linux used Clang18.1.3. PR-013 is eligible; no PR-012 implementation or validation work remains.

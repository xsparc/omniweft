# PR-008 slice plan: offline AI blocks showcase

- Adopted scope: [authorization](AUTHORIZATION.md), roadmap PR-008 and [showcase.ai_blocks](../../examples/showcase-ai_blocks.md).
- Owner: Codex coordinator, branch codex/pr-008-ai-blocks, base 1c94777a9fac7869cc83e21f01b1ccc034c439a4. Existing worktrees remain preserved.
- Dependencies: PR-004 and PR-006 previously integrated; PR-007 and its compatibility repair now integrated with all six post-merge checks passing.
- Behavior: a repository-authored Python script builds a small open room and rearranges its furnishings using the public PolicySession/PolicyClient. The native policy host renders detached committed snapshots on its main thread while its existing fixed-step owner and policy gateway run independently.
- Bounds: use only the existing east principal, at most five cubes (1920 retained charged bytes of its 2048-byte budget), at most four operations per transaction, and existing deadlines/queue/observation limits. No grant, quota or permission changes. No generated code, live model, API key, new asset or dependency.
- Native changes: add opt-in output/GPU/interactive support to the existing policy host, share report serialization with the agents host, and give the renderer a host-owned two-revision capture schedule with existing defaults retained. Do not duplicate or alter owner scheduling or policy admission/lease logic.
- SDK changes: additive PolicySession presentation options; no wire protocol, save format, credential flow or retry behavior change. The example process is the scripted agent and remains separate from the native host.
- Fixture: seed 7, floor/back/side shell and two furnishings within the east region. Construct via three bounded transactions; hold assembled revision 3 until actually presented; reject an atomic plan that stages a permitted edit followed by a forbidden-region edit; verify complete state and quota recovery; commit rearrangement revision 4 and wait for actual presentation. Exact coordinates and independent expectations are frozen before validation.
- Independent oracle: complete snapshots/handles/authoring revisions and receipts, OWOBJ001 canonical state equality before/after rejection, policy scope and idle usage, successful recovery. GPU oracle independently ray-tests fixed scene geometry and compares color/object-ID/depth attachments plus actual source/publication/presentation provenance. Oracle expectations cannot import implementation fixture values.
- Negative evidence: reject the invalid plan with exact error/operation, unchanged full state and no retained/working/request leak; demonstrate oracle rejection of altered state and pixel artifacts. Include meaningful source mutation proof where practical.
- Lanes: Windows/Linux CPU builds and structural checks; Windows actual physical GPU with core/synchronization validation and deterministic pixel thresholds; conservative cached Linux Docker with two CPUs/3 GiB, no network/host mounts. Linux physical GPU remains not_run unless genuinely available; no container result implies desktop GPU support.
- Commands/artifacts: existing pinned CMake/Ninja/CTest plus a Python example CLI and independent showcase oracle, with fresh output directories and candidate-bound allowlisted manifests. Preserve credentials/epochs, raw streams, local paths and binaries privately.
- Review/ownership: coordinator owns all changes and ledger; independent architecture, implementation and evidence review required before delivery. Architecture review completed; source/oracle reviewers resolved the policy runtime presentation serializer, readback retention, numeric privacy bounds and build/shader provenance gaps. No merge or contribution certification is fabricated.
- Rollback: ordinary PR revert; no migrations, public protocol change or new dependency. Runtime and renderer regressions require existing affected checks in addition to the showcase oracle.
- Risks: policy observation cap and renderer capture sequencing must accommodate the full five-object state; GPU startup/shutdown and final publication must remain bounded. Failures are evidence, never relabeled success.

## Frozen geometry

All coordinates/scales are meters. Floor position (2.5,-1.1,0), scale (2.5,.2,2); back wall (2.5,.2,-.9), (2.5,2.4,.2), quaternion (0,1,0,0); side wall (1.35,.2,0), (.2,2.4,2); table (2.15,-.55,.35), (.8,.9,.7), quaternion (0,.6,0,.8); stool (3.2,-.7,.5), (.4,.6,.5). Other rotations are identity. The corrected table destination is (3.1,-.55,.15), stool destination (2,-.7,.5); invalid stool destination (0,-.7,.5). These values were independently reviewed before GPU testing.
